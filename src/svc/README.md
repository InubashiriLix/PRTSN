# service

System services live here.

Use this layer for WiFi, MQTT, OTA, time sync, telemetry publishing, command subscriptions, and other reusable services that are above raw firmware peripherals but below controller orchestration.
